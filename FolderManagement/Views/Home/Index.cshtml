@model IEnumerable<Folder>

@{
    ViewBag.Title = "Folder List";
}

<div class="card-deck">
    @foreach (var folder in Model)
    {
        

        <div class="card m-3" style="min-width:18rem; max-width:30.5%">
            <div class="card-header">
                <h3> @folder.Name</h3>
            </div>
           
            @*<div class="card-footer text-center">
                <a asp-controller="home" asp-action="details" asp-route-id="@folder.FolderId" class="btn btn-primary m-1">View</a>

                <a asp-action="edit" asp-controller="home"
                   asp-route-id="@folder.FolderId" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete TestCase</a>
            </div>*@
        </div>

    }
</div>
